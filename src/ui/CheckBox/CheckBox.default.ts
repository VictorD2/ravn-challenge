import { FontType, SeparatorType, SizeType } from "@/styles/types";

export const defaultFont: FontType = {
  size: "text-xs",
  color: "text-white",
};

export const defaultSize: SizeType = {
  width: "w-full",
};

export const defaultSeparator: SeparatorType = {};
